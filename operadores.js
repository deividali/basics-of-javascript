// suma
const a=1;
const b=2;
const e="2";
c = a + b;
// comparo si las dos variables contienen el mismo valor
console.log("suma: ",b==e); 
// comparo si las dos variables contienen el mismo valor y el mismo tipo de dato
console.log("suma: ",b===e);
